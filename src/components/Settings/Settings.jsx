import React from 'react';
// import style from './Settings.module.css';

export const Settings = (props) => {
      return (
            <div>
                  <div>Settings</div>
            </div>
      );
};
